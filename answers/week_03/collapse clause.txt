Parallelisierung von miteinander verknüpften for-Schleifen (Sonst würde nur die erste folgende for-Schleife parallelisiert werden)
Collapse(2) besagt, dass die zwei folgenden for-Schleifen zu einer for-Schleife kollabiert werden.
Code: #pragma omp parallel for collapse(2)
